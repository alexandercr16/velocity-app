// Fill these in from your own Firebase project:
// Firebase console → ⚙️ Project settings → General tab → "Your apps" → SDK setup and configuration.
// These values are safe to commit to a repo — Firebase access is controlled by
// security rules (see firestore.rules), not by keeping this config secret.
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
